export enum Paths {
  getUsers = '/api/users',
}

export enum Method {
  DELETE = 'DELETE',
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
}

export enum StatusCode {
  success = 200,
  successfullyCreated = 201,
  noContent = 204,
  badRequest = 400,
  notFound = 404,
}
